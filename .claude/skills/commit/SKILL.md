# Atomic Commit
1. Run `git status` and `git diff`
2. Group changes into atomic logical commits
3. For each: stage selectively, run pytest, write conventional commit message
4. Never combine unrelated changes
