# Backlog Workflow
Read proposals.md (or BACKLOG.md). For each item:
1. Discuss tradeoffs with user before implementing
2. Confirm scope (which sub-parts to implement)
3. Implement, run full test suite, commit atomically
4. Remove completed item from backlog
