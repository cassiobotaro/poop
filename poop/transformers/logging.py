from poop.types.logging import Formatter, Handler, Logger, Logging

NAMESPACE: dict[str, object] = {
    "logging": Logging,
    "Logger": Logger,
    "Handler": Handler,
    "Formatter": Formatter,
}
