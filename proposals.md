# Proposals

Open design backlog. Closing convention: see [`CONTRIBUTING.md`](CONTRIBUTING.md#closing-a-proposal).

The backlog is empty. Items 42–61 have been implemented and closed by removal,
which is the second of the two closing conventions; the decisions and the
reasoning behind them live in [`INFECTIONS.md`](INFECTIONS.md), and the diffs
live in the git history.

Numbering continues from the closed set, so a `proposal N` in a code comment
keeps pointing at one entry and never at two. Open the next one below.
